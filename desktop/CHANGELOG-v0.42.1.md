# v0.42.1

Hotfix:
- Fixed startup crash in `_apply_styles`.
- The new `PendingBalance` Qt stylesheet block now escapes literal braces correctly inside the Python f-string.
- No firmware change required.
