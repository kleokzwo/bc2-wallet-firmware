# v0.41.0

- Version synchronized with Desktop v0.41.0.
- Keeps the v0.40.0 hardware receive-derivation fix and explicit wallet-lock USB command.
- Primary v0.41.0 transport fix is on the Desktop side: robust parsing of BC2 frames in the shared ESP-IDF console/protocol serial stream.
